# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Local Claude Code state is ignored, except project skills — those are shared
# guardrails and must travel with the repo. (Exclude the contents, not the
# directory itself: git cannot re-include a path inside an excluded directory.)
.claude/*
!.claude/skills/
.vercel

# Obsidian vault workspace state (docs/ opens as a vault)
.obsidian/
